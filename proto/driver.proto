syntax = "proto3";

package driver;

service DriverService {
  rpc RegisterDriver(RegisterDriverRequest) returns (Driver);
  rpc GetDriver(GetDriverRequest) returns (Driver);
  rpc ListAvailableDrivers(ListAvailableDriversRequest) returns (ListDriversResponse);
  rpc UpdateLocation(UpdateLocationRequest) returns (Location);
  rpc DeleteDriver(DeleteDriverRequest) returns (DeleteDriverResponse);

  // Server-streaming : pousse la position du livreur a chaque mise a jour
  rpc StreamDriverLocation(StreamLocationRequest) returns (stream Location);
}

enum DriverStatus {
  DRIVER_STATUS_UNSPECIFIED = 0;
  AVAILABLE = 1;
  BUSY = 2;
  OFFLINE = 3;
}

message Location {
  string driver_id = 1;
  double latitude = 2;
  double longitude = 3;
  double speed_kmh = 4;
  double heading_deg = 5;
  string timestamp = 6;
}

message Driver {
  string id = 1;
  string name = 2;
  string phone = 3;
  string vehicle_type = 4;
  DriverStatus status = 5;
  string current_order_id = 6;
  Location last_location = 7;
  string created_at = 8;
  string updated_at = 9;
}

message RegisterDriverRequest {
  string name = 1;
  string phone = 2;
  string vehicle_type = 3;
}

message GetDriverRequest {
  string id = 1;
}

message ListAvailableDriversRequest {
  int32 limit = 1;
}

message ListDriversResponse {
  repeated Driver drivers = 1;
  int32 total = 2;
}

message UpdateLocationRequest {
  string driver_id = 1;
  double latitude = 2;
  double longitude = 3;
  double speed_kmh = 4;
  double heading_deg = 5;
}

message StreamLocationRequest {
  string driver_id = 1;
}

message DeleteDriverRequest {
  string id = 1;
}

message DeleteDriverResponse {
  bool deleted = 1;
}
